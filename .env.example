# Sample environment variables for the Telegram bot
# Rename to .env and fill in your bot token before running `python -m bot.main`
BOT_TOKEN="123456:example-token-replace-me"
# Optional path to the SQLite storage file
BOT_STORAGE="./var/bot.sqlite"
